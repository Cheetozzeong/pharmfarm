# PharmFarm Design Rules

이 파일은 PharmFarm 웹/모바일 UI를 만들거나 수정할 때 반드시 따르는 디자인 기준이다. 이 앱은 약국 재고 스캔, 입고, 반품 확인을 빠르게 처리하는 업무형 도구이므로 장식보다 가독성, 대비, 터치 정확도, 상태 피드백을 우선한다.

## 1. Typography

- 기본 서체는 `Pretendard`를 사용한다. 새 UI를 만들 때 `Inter`, `Roboto` 같은 일반 기본값을 무심코 추가하지 않는다.
- 폰트 크기는 임의 값으로 찍지 않는다. 아래 모듈러 스케일 토큰을 먼저 사용하고, 예외는 기존 컴포넌트와 맞출 때만 둔다.

```css
:root {
  --font-xs: 0.8rem; /* 12.8px */
  --font-sm: 0.875rem; /* 14px */
  --font-md: 1rem; /* 16px */
  --font-lg: 1.25rem; /* 20px */
  --font-xl: 1.563rem; /* 25px */
  --font-2xl: 1.953rem; /* 31px */
}
```

- 본문과 입력값은 `--font-md`, 보조 설명은 `--font-sm`, 작은 배지는 `--font-xs`, 화면 제목은 `--font-lg` 이상을 사용한다.
- 업무 화면의 조작 텍스트는 viewport 기준으로 흔들리게 만들지 않는다. 넓이에 따라 조정이 필요하면 컨테이너 쿼리와 `clamp()`를 함께 쓰고, 반드시 최소/최대값을 둔다.
- 줄 높이는 역할별로 정한다. 작은 라벨과 칩은 `1.2`부터 `1.3`, 본문은 `1.45`부터 `1.6`, 제목은 `1.15`부터 `1.25`를 사용한다. 모든 요소에 `line-height: 1.5`를 일괄 적용하지 않는다.

## 2. Color And Contrast

- 새 색상 토큰은 가능하면 OKLCH로 정의한다. 기존 hex 토큰을 건드릴 때도 명도와 채도를 의도적으로 맞춘다.
- 순수 검정 `#000000`과 순수 흰색 `#ffffff`를 대형 배경으로 남발하지 않는다. 밝은 배경은 살짝 틴트가 있는 뉴트럴, 어두운 배경은 아주 짙은 틴티드 뉴트럴을 사용한다.
- 회색은 무채색 회색이 아니라 브랜드 블루나 의료 도메인의 청록 기운이 아주 약하게 섞인 tinted neutral을 쓴다.
- 일반 텍스트와 배경의 대비는 최소 `4.5:1`, 큰 제목은 최소 `3:1`을 맞춘다. 컬러 배경 위에 저대비 회색 글씨를 올리지 않는다.
- 상태는 색상만으로 전달하지 않는다. 아이콘, 텍스트, 배지 문구를 함께 사용한다.
- 다크 모드는 라이트 모드의 단순 반전이 아니다. 배경은 순수 검정이 아닌 어두운 회색 계열을 쓰고, 브랜드 컬러는 채도를 낮춰 눈부심을 줄인다.

```css
:root {
  --brand: oklch(58% 0.21 255);
  --brand-soft: oklch(96% 0.025 255);
  --surface: oklch(98% 0.006 230);
  --surface-muted: oklch(94% 0.01 230);
  --text: oklch(22% 0.018 230);
  --text-muted: oklch(54% 0.025 230);
  --line: oklch(90% 0.012 230);
}

.is-dark {
  --brand: oklch(68% 0.14 255);
  --surface: oklch(15% 0.015 230);
  --surface-muted: oklch(20% 0.018 230);
  --text: oklch(94% 0.008 230);
  --text-muted: oklch(72% 0.018 230);
  --line: oklch(30% 0.016 230);
}
```

## 3. Spatial Design

- 모든 여백, 패딩, gap, inset은 4px 기반 스케일을 사용한다. 기본 단위는 `4, 8, 12, 16, 24, 32, 40, 48`이다.
- 카드 안에 카드를 넣는 중첩 구조를 만들지 않는다. 카드가 필요한 곳은 반복 리스트 항목, 모달, 실제로 경계가 필요한 도구 영역뿐이다.
- 화면 섹션은 넓은 band 또는 unframed layout으로 구성한다. 불안하다는 이유로 모든 콘텐츠를 둥근 박스 안에 넣지 않는다.
- 업무 화면은 정보 밀도를 유지하되, 주요 액션 주변에는 최소 `16px` 이상의 여백을 둔다.
- 컴포넌트 재사용성이 필요한 레이아웃은 viewport 미디어 쿼리보다 container query를 우선한다.

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --radius-sm: 6px;
  --radius-md: 8px;
}
```

## 4. Motion

- 애니메이션은 상태 변화를 이해시키는 데만 사용한다. 장식용 bounce, 흔들림, 과한 hover 효과를 넣지 않는다.
- 기본 duration은 `150ms`부터 `250ms` 사이로 둔다. 등장에는 `ease-out`, 사라짐에는 `ease-in`, 상태 전환에는 부드러운 custom easing을 사용한다.
- 애니메이션 대상은 `transform`과 `opacity`로 제한한다. `width`, `height`, `margin`, `top`, `left`를 움직여 레이아웃 리플로우를 만들지 않는다.
- 스캔 라인, 토스트, 모달, 드롭다운을 수정할 때도 이동은 `translate` 기반으로 처리한다.
- `prefers-reduced-motion: reduce`를 지원한다.

```css
:root {
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in: cubic-bezier(0.7, 0, 0.84, 0);
  --ease-standard: cubic-bezier(0.2, 0, 0, 1);
  --duration-fast: 150ms;
  --duration-base: 220ms;
}
```

## 5. Interaction

- `outline: none`으로 포커스 표시를 제거하지 않는다. 키보드 사용자를 위해 `:focus-visible`을 명확하게 남긴다.
- 모바일 터치 타겟은 최소 `44px` by `44px`를 보장한다. 아이콘 버튼도 예외가 아니다.
- 아이콘 버튼에는 `aria-label`을 제공한다. 낯선 아이콘에는 tooltip이나 보조 텍스트를 제공한다.
- 로딩은 가능하면 스피너만 쓰지 않는다. 리스트, 카드, 입력 결과에는 skeleton UI를 우선하고, 되돌릴 수 있는 작업에는 optimistic update를 검토한다.
- 비활성화된 버튼은 왜 누를 수 없는지 주변 상태나 문구로 알 수 있어야 한다.
- 파괴적 행동은 명확한 문구와 확인 단계를 둔다.

## 6. Responsive Design

- CSS는 모바일 퍼스트로 작성하고, 넓은 화면은 `min-width` 미디어 쿼리로 확장한다.
- 최소 폭 `320px`에서도 텍스트가 버튼이나 칩 밖으로 넘치지 않아야 한다.
- 고정 형식 UI, 예를 들어 스캔 화면, 툴바, 하단 탭, 카운터, 리스트 행은 `min-height`, `aspect-ratio`, grid track 등 안정적인 크기 제약을 둔다.
- 모바일 WebView와 브라우저 주소창 변화를 고려해 `svh`, `dvh`, safe-area inset을 사용한다.
- 텍스트가 길어지는 한국어 문구는 줄바꿈, 말줄임, max-width를 명확히 지정한다.

## 7. UX Writing

- 버튼 문구는 행동 결과를 구체적으로 쓴다. `확인`, `제출`, `처리`보다 `입고 확정`, `반품 수량 저장`, `공급처 다시 조회`처럼 작성한다.
- 빈 상태는 다음 행동을 제안한다. `데이터 없음`만 표시하지 않는다.
- 오류 메시지는 원인과 다음 행동을 함께 말한다. 예: `공급처를 찾지 못했습니다. 바코드를 다시 스캔하거나 직접 선택하세요.`
- 약국 업무 맥락에서는 과장된 마케팅 문구보다 짧고 정확한 안내를 우선한다.
- 숫자, 수량, 금액, 유효기간, 보험코드는 스캔하기 쉬운 형태로 정렬하고 단위를 생략하지 않는다.

## Anti-Slop Checklist

새 UI를 완료하기 전 아래 항목을 확인한다.

- 임의 폰트 크기, 임의 line-height, 새 기본 폰트를 추가하지 않았다.
- 보라-파랑 네온 그라데이션, 의미 없는 장식 배경, 과한 glow를 쓰지 않았다.
- 카드 안의 카드 구조를 만들지 않았다.
- 순수 검정 배경이나 컬러 배경 위 저대비 회색 글씨를 쓰지 않았다.
- `transition: all`을 쓰지 않았다.
- 레이아웃 속성을 애니메이션하지 않았다.
- 포커스 링과 키보드 이동을 보존했다.
- 모바일 터치 타겟이 최소 44px를 만족한다.
- 버튼, 빈 상태, 오류 문구가 다음 행동을 명확히 말한다.
- 화면이 320px 모바일 폭과 데스크톱 폭 모두에서 겹침 없이 읽힌다.
